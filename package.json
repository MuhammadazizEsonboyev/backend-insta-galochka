{
  "name": "demoserver",
  "version": "1.0.0",
  "description": "Backend server for Render deployment",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",                  
    "dev": "json-server --watch db.json --port 3000",               
    "build": "echo 'No build needed for backend server'" 
  },
  "keywords": ["backend", "server", "render"],
  "author": "Muhammadaziz Esonboyev",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.2"
  },
  "engines": {
    "node": "20.x"                            
  }
}
